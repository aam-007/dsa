#include <iostream>
#include <cstddef>

namespace lds{
    constexpt std::size_t MAX {10};
    enum class STATUS{
        OVERFLOW,
        UNDERFLOW,
        OK
    };

    struct circularQueue{

    };
}
//to finish
